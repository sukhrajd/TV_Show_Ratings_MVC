@model TV_Show_Ratings_MVC.Models.TVShow

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TVShow</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="TVChannelId" class="control-label"></label>
                <select asp-for="TVChannelId" class="form-control" asp-items="ViewBag.TVChannelId"></select>
                <span asp-validation-for="TVChannelId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShowName" class="control-label"></label>
                <input asp-for="ShowName" class="form-control" />
                <span asp-validation-for="ShowName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
